V8|`.:code:signatures` emits the `///` lines into the worker's surface, & `.:code:B4` is a judge that could ⊥ tell whether `not_owns` held a path or prose w/o them ∴ ONE undocumented `pub fn` degrades every prompt built from that node afterwards — the loop's own next run included. scoped to NEW fns like every rule here: 69 undocumented public items already exist in this tree & are a separate debt, & flagging them would fire on work nobody just did. FOUND in `.:tdd:T13`'s first merit win, which shipped `post_with_retry` undocumented past two gates & a blind judge
