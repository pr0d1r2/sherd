# Per-path token CHAIN ceilings, gated by `sherd budget`. itok's format,
# reused rather than reinvented -- a second format for one job would be two
# readings of one rule.
#
# WHAT THIS CEILING IS: the cost of REACHING a node -- root plus every
# ancestor plus itself -- which is what a worker pays before it sees any code.
# Measured on the target tier (gpt-oss:20b, 131,072 window, 28,543 entry cost,
# 102,529 working), the spec share is ~25%, so ~25,000 tokens.
#
# It forces a REVIEW, not a shrink. Set with real slack (~15-30%): a ceiling a
# hair above current turns every legitimate addition into a raise, and a
# threshold raised reflexively trains the reflex instead of catching the
# problem. Raises belong in a commit with their reason.
#
# RESET 2026-08-19 (T75), ~25% above measured, after T41 made `--depth rule`
# select and every chain fell ~35%. The numbers these replace were set against
# packs that carried §R and §B, so they gated nothing: `src/tdd` sat at 10,649
# under a 16,000 ceiling and could have grown by half unnoticed.
#
# Every node carries its own row now. `src/land` and `src/slice` inherited
# `src` by longest prefix, which IS checked (V104) -- but it left them less
# slack than their parent, which reads as deliberate and was not.
#
# Measured 2026-08-19, `sherd budget`: root 8,401 · src 8,527 · deepest
# src/tdd 10,649.
#
# RESET 2026-08-23, and this one is a RESTORATION rather than a raise. The
# rule three paragraphs up asks for 15-30% slack; measured against the
# numbers below it, every chain had between 0.0% and 2.5%, and four sat
# EXACTLY at their ceiling. T75's 25% was consumed by four days of real work
# -- CI, the hygiene family, six linters, T71 -- so the instrument had
# quietly become a ratchet on prose: a truthful one-line §C edit put four
# chains over, and the only way to record a guard was to delete words
# elsewhere.
#
# That is the failure the paragraph above names, arrived at from the other
# direction. A ceiling with no slack does not force a review; it forces a
# workaround, and the workaround is the reflex this file exists to prevent.
#
# The 20% is measured, not chosen for comfort: the largest chain after this
# reset is `src/tdd` at 16,000, which is 15.6% of the 102,529 working tokens
# the target tier leaves -- still inside the ~25% spec share this file's
# header sets as the budget. The binding constraint stayed where R41 found
# it, on competence rather than context.
#
# Measured 2026-08-23, `sherd budget`: root 10,399 · src 10,525 · deepest
# src/tdd 13,296.

# RAISE 2026-08-23 (second today), `dev` 13000 -> 15500 and `src/plan` 13800
# -> 17500. Both are RESTORATIONS of the 15-30% slack this file asks for:
# measured immediately before the edit that triggered them, `dev` sat at
# 12,987 of 13,000 (0.1%) and `src/plan` at 13,644 of 13,800 (1.1%). The
# morning's reset gave every node ~20% and two nodes had spent all of it by
# evening, which is the same finding as the reset itself, one day compressed
# into one day.
#
# What was added: root V118 (the three layers of idempotence) with B20, and
# four invariants to `src/plan` -- V18 feedback idempotence, V19 qualified
# citations, V20 spread-the-numbers-centralize-the-ledger, V21 source
# anchors as a migration and split signal.
#
# `src/plan` at 17,500 becomes the largest ceiling in the file, passing
# `src/tdd`'s 16,000. That is 17% of the 102,529 working tokens the target
# tier leaves -- still inside the ~25% spec share the header sets, and still
# a fact worth reading rather than approving: `src/plan` now owns two
# subjects, planning steps and proposing federation, and its own new V21
# ranks `src/plan/mod.rs` second in the tree by distinct foreign anchor
# owners. The ceiling raise is evidence for `src/plan:T14`, not a
# substitute for it.

# RAISE 2026-08-23, `src/cli` 13300 -> 15500, third of the day and the last
# node still holding the old margin: it measured 13,278 of 13,300 (0.2%)
# before the edit that pushed it over.
#
# What pushed it: `src/spec:B2` made the canonical citation the node PATH, so
# twenty-four bare `tdd:B12` citations across the tree became `src/tdd:B12`.
# The fix costs tokens by design -- a citation that resolves is longer than
# one that does not -- and `src/cli` carries more of them than any other node
# because it is where every check prints its coordinates. `src/cli:V14` is the
# other few tokens: the rule four files had cited as `.:V41` since the first
# commit, which was never written down anywhere.
#
# And `src/spec` 13300 -> 15500 in the same commit, for the same reason: it
# was at 13,197 of 13,300 (0.8%) and it is the node that gained V7 and the
# closed B2. Every node this file has raised today was inside 1.2% of its
# ceiling before it was touched, which says the morning's 20% was measured
# against a tree that then had a working day happen to it.

# RESET 2026-08-23, third edit today and the only one that goes DOWN. Every
# ceiling is ~22% above the chain measured after `src/fed:V9` was enforced:
# 106 finished `§T` rows were deleted across the tree, 45 of them at root.
#
# A `§T` row states REMAINING work, and rule depth loads `§T`, so every
# finished row was billed to every chain on every turn. Root fell 12,420 ->
# 9,922 and the repo fell 228,835 -> 184,174 tokens, about 19%.
#
# This supersedes the three raises made earlier today. `dev`, `src/cli`,
# `src/plan` and `src/spec` were raised to 15,500-17,500 within hours of
# hitting their ceilings, and each is now back under 13,000 without any of
# the law added today being removed. The raises were real at the time and
# the reason they stopped being necessary is worth more than the raises
# were: the pressure was history, not law.
#
# Every chain is now under 13,107 -- ten percent of the 131,072 window,
# which is the boundary the target tier makes meaningful. Six were over it
# this morning. The largest is `src/plan` at 12,385, and `src/plan:T14`
# still stands: that node owns two subjects and the split is worth making
# on its own terms rather than because a ceiling forced it.
SPEC.md        12200
dev            13000
src            12300
src/assay      13600
src/cli        13200
src/code       13200
src/debt       13700
src/fed        13300
src/land       13400
src/lens       12800
src/ollama     14000
src/plan       15200
src/review     13000
src/slice      12900
src/spec       13200
src/state      12500
src/tdd        15100
src/tokens     12800
#
# `src/debt` added 2026-08-24 at 13,700, ~22% over its measured 11,262 --
# the same slack every other row carries. It inherited `src` by longest
# prefix, which IS checked (V104), and that left it 8% where the header asks
# for 15-30%.
#
# NOTE, because it corrects a claim made when the node was proposed: moving
# six §B rows down did NOT relieve any chain. Rule depth loads §G §C §I §V §T
# and NOT §B or §R, so a bug row has never cost a chain a single token. Root
# went 10,348 -> 10,350. What the promotion bought is one reading of the
# gate's inputs and history filed where it happened -- not budget.
